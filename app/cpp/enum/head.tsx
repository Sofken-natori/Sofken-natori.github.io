'use strict';

import { CPPEntHead } from '../../components';
import type { JSX } from 'react';

export default function CPPEntEnumHead(): JSX.Element {
    return (
        <CPPEntHead slug="enum" />
    );
}
